Functions for plotting
**********************

Most of these functions can be called as methods of a `MutationCaller object <code_mutation_calling.rst>`_, but to make it more straightforward to analyse any set of (external) mutations, they can also be called individually.

.. automodule:: isomut2py.plot
   :members:

